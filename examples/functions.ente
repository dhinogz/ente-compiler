program functions;
var x : int;
    y : float;

void foo(x : int) {
    var z : int;
    z = x + 2;
    write(z);
}

void bar(x : int, y : float) {
    var z : int;
    z = x + y;
    {
        z = x + y;
        foo(z - 1);
        write(z);
    }
}

main {
    x = 5;
    y = 2.5;
    write(x);
    write(y);
};

end;
